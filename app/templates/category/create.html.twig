{% include 'default/_form.html.twig' with {
    page_title: 'title.category_create'|trans({'%id%': category.id|default('')}),
    form: form,
    submit_label: 'action.create'|trans,
    back_to_list_path: 'category_index',
} %}
