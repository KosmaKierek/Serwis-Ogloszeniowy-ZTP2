{% include 'default/_form.html.twig' with {
    page_title: 'title.advert_create'|trans({'%id%': advert.id|default('')}),
    form: form,
    submit_label: 'action.create'|trans,
    back_to_list_path: 'advert_index',
} %}